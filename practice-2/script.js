gsap.from("h1",{
    opacity:0,
    duration:2,
    delay:1,
    y:30 ,
    stagger:0.5   //stagger is use to run every element one by one 
    //giving stagger as -1 will reverse the order of elements
})